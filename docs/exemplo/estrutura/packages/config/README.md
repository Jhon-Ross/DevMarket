# packages/config — Configurações

Responsabilidades
- ESLint, Prettier, tsconfig base, Tailwind/PostCSS.
- Regras e padrões comuns do monorepo.

Decisões
- TypeScript `strict`.
- Import/order, unused vars, segurança.