/devmarket (monorepo — exemplo de estrutura)
  apps/
    web/                      # Next.js (App Router, ISR, NextAuth, Stripe)
      src/
        app/
          layout.tsx          # Root layout + ThemeProvider
          page.tsx            # Landing/placeholder
          (public)/
            dev/
              [slug]/
                page.tsx      # Página pública de perfil (SSG/ISR)
            project/
              [slug]/
                page.tsx      # Página pública de projeto (SSG/ISR)
          api/
            webhooks/
              sanity/route.ts # Revalidação ISR via webhook
              stripe/route.ts # Webhook para assinaturas
        components/           # Containers e componentes específicos do app
        styles/
          theme.css           # CSS variables (tokens semânticos)
          globals.css         # Reset, tipografia, utilidades
      README.md
    studio/                   # Sanity Studio
      schemas/
        userProfile.ts
        project.ts
      README.md

  packages/
    ui/                       # Design system (presentational, sem chamadas de dados)
      src/
        components/
          Button.tsx
          Card.tsx
          Avatar.tsx
          Tag.tsx
          Grid.tsx
          MediaGallery.tsx
        index.ts
      README.md
    sanity/                   # Cliente, queries GROQ, helpers tipados
      src/
        client.ts
        queries/
          userProfile.ts
          project.ts
      README.md
    lib/                      # Utilitários (env, rate-limit, formatadores)
      src/
        env.ts
        seo.ts
      README.md
    types/                    # Tipos e contratos (DTOs, domínio)
      src/
        content.ts
        user.ts
      README.md
    config/                   # ESLint, Prettier, tsconfig base, Tailwind/PostCSS
      eslint.config.js
      prettier.config.js
      tsconfig.base.json
      tailwind.config.js
      README.md

  prisma/
    schema.prisma             # User, Subscription, vínculos Sanity
    README.md

  .env.example                # SANITY_*, STRIPE_*, DB, NEXTAUTH_*
  turbo.json                  # Pipelines de build/lint/test
  package.json                # Scripts